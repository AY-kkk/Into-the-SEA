# 生成式 AI 内容免责声明与合规说明

## 免责声明

- 模拟面试的提问、追问、评分与报告，以及部分学习建议，由**大语言模型自动生成**，可能存在不准确、不完整或过时之处。
- 上述内容**仅供备考参考，不构成任何official建议或录用承诺**。招录政策、公告、报名时间等**以官方发布为准**。

## 内容合规（面向中国大陆上线）

- 依据《生成式人工智能服务管理暂行办法》，正式上线须：
  1. 完成**算法备案 / 生成式 AI 服务备案**（TODO：上线前办理）。
  2. 对生成内容进行**审核与安全过滤**（✅ 已实现本地规则引擎 `src/lib/moderation`：违规词拦截 + 个人敏感信息(PII)脱敏，覆盖面试输入/输出、简历文本；远程内容安全服务 `MODERATION_PROVIDER=real` 骨架已预留）。
  3. 在展示 AI 生成内容处**显著标识「AI 生成」**（前端已在面试模块标注，需持续覆盖）。
- 用户输入与模型输出的日志留存与去标识化处理须符合个保法要求。

## 技术护栏（已实现）

- LLM 单次 `max_tokens` 上限（`LLM_MAX_TOKENS_CAP`）。
- 单用户每日调用配额（`LLM_DAILY_CALL_CAP`）+ 接口限流，防滥用与成本失控。
- Provider 抽象层：真实模型不可用/缺凭据时自动降级 mock，保证可用性。

_本说明为占位模板，正式上线前须由法务/合规确认。_
